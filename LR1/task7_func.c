#include "func.h"
#include <math.h>
float func(void)
float func(void)
{
	f = pow(cos(x), 4) + pow(sin(y), 2) + 0, 25 * pow(sin(2 * x), 2) - 1;
}

Исходный код файла func.h
#pragma once

float func();
float x, y, f;
